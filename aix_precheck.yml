---
- hosts: localhost
  remote_user:
  gather_facts: yes

  tasks:
    - name: Run bosboot & bootlist
      bosboot:
        name: run
        bootlist: True
      delegate_to: "127.0.0.1"
      register: result
      become: true

    - name: debug
      debug:
        var: result
