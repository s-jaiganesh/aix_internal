---
- hosts: localhost
  remote_user:
  gather_facts: yes

  tasks:
    - name: emgr preview - all lables
      emgr:
        label: all
        state: absent
        preview: True
      register: emgr
      become: true

    - name: debug boot
      debug: var=emgr
